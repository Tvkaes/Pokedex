// Re-export parsing utilities from pkmn-core for backward compatibility
export {
  formatPokemonName,
  formatPokemonId,
  extractDescription,
  extractGenus,
  extractNativeName,
  extractLocalizedDisplayName,
  mapStats,
  selectFeaturedAbility,
} from '@tvkaes/pkmn-core'
